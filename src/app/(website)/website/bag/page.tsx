export default function BagPage() {
  return (
    // <section className="main-container py-10 flex flex-col justify-center items-center gap-8 ">
    //   <div className="grid lg:grid-cols-4 md:grid-cols-2 grid-cols-1 md:gap-10 gap-5 w-full ">
    //     {bagProducts?.map((item, i) => (
    //       <Link
    //         href={`/website/product-details?category=bag&id=${item?.id}`}
    //         key={i}
    //       >
    //         <ProductCard item={item} />
    //       </Link>
    //     ))}
    //   </div>
    // </section>
    <section className="main-container py-40 text-primary font-medium text-4xl uppercase flex justify-center ic">
      COMING SOON
    </section>
  );
}
